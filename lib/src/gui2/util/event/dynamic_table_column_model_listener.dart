// Translation of pcgen.gui2.util.event.DynamicTableColumnModelListener

/// Listener for changes to a dynamic table column model.
abstract class DynamicTableColumnModelListener {
  void columnAdded(dynamic event);
  void columnRemoved(dynamic event);
  void columnMoved(dynamic event);
  void columnMarginChanged(dynamic event);
  void columnSelectionChanged(dynamic event);
}
