// Translation of pcgen.gui2.tabs.models.TextFieldListener

/// Listener interface for text field changes in character tab models.
abstract class TextFieldListener {
  void textFieldChanged(String fieldName, String newValue);
}
