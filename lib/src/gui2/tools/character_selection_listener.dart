// Translation of pcgen.gui2.tools.CharacterSelectionListener

/// A callback interface for components that need to respond to character
/// selection changes.
abstract class CharacterSelectionListener {
  /// Called when the active character changes.
  void setCharacter(dynamic character);
}
