// Translation of pcgen.io.Compatibility

/// Checks compatibility of PCG file versions with the current application.
class Compatibility {
  Compatibility._();

  /// Returns true if the given version string is compatible.
  static bool isCompatible(String version) {
    // TODO: implement version compatibility checks
    return true;
  }
}
